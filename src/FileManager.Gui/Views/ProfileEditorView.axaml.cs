using Avalonia.Controls;

namespace FileManager.Gui.Views;

/// <summary>The Profile editor view (full §5.1 schema, safety banners, validation, save).</summary>
public sealed partial class ProfileEditorView : UserControl
{
    /// <summary>Initializes the view from its XAML.</summary>
    public ProfileEditorView() => InitializeComponent();
}
