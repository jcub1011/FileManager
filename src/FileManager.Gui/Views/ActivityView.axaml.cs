using Avalonia.Controls;

namespace FileManager.Gui.Views;

/// <summary>The live activity/error view: recent Jobs with per-Job log drill-down.</summary>
public sealed partial class ActivityView : UserControl
{
    /// <summary>Initializes the view from its XAML.</summary>
    public ActivityView() => InitializeComponent();
}
